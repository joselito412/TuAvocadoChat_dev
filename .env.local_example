# Claves de Supabase (Ya configuradas)
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"

# Clave de Verificación de Webhook (Capa 1 - Handshake)
WHATSAPP_VERIFY_TOKEN="your_secure_whatsapp_verify_token"

# --- CLAVES CRÍTICAS DE INTERACCIÓN (CAPA 4/5) ---
# Token de Acceso de Meta (CRÍTICO para enviar mensajes)
WHATSAPP_API_TOKEN="[TU_ACCESS_TOKEN_LARGO_DE_META]"

# ID de tu Número de Teléfono de WhatsApp Business (CRÍTICO para construir la URL de envío)
WHATSAPP_PHONE_ID="[TU_PHONE_NUMBER_ID]"